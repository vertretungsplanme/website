build:
    script:
        - npm install
        - npm install -g gulp
        - gulp build-ls
        - gulp build-main
    artifacts:
        paths:
            - _site/ls_vertretungsplan_me
            - _site/vertretungsplan_me